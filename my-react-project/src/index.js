import React from 'react';
import ReactDOM from 'react-dom/client';
import FetchCustomHooks from './Pages/FetchCustomHooks';






const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
     <React.StrictMode>
        <FetchCustomHooks/>
     </React.StrictMode>
);

